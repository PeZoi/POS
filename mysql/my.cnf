[mysqld]
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci

# MySQL 8 default auth plugin is fine for Connector/J 8.
# Keep config minimal; tune per workload on VPS.

[client]
default-character-set = utf8mb4

